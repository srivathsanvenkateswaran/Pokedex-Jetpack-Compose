<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pokedexjetpackcompose">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".PokedexApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_poke_ball"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_poke_ball"
        android:supportsRtl="true"
        android:theme="@style/Theme.PokedexJetpackCompose">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.PokedexJetpackCompose.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>